<li id="micropost-<%=post.id%>">
	<%= link_to gravater_for(post.user.size :50), post.user%>
	<span class ="user"><%= link_to micropost.user.username, post.user%></span>
	<span class = "content"> <% micropost.content %> </span>
	<span class = "timestamp"> < Posted<%= time_ago_in_words(micropost.created_at)%> ago
	</span>
</li>
